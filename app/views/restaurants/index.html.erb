<% @restaurants.each do |restaurant| %>
  <div class="card">
    <h3><%= restaurant.name %></h3>
    <p><%= restaurant.address %></p>
    <p><%= restaurant.phone_number %></p>
    <%= link_to 'Check Restaurant', restaurant_path(restaurant), class: "btn btn-primary" %>
  </div>
<% end %>
